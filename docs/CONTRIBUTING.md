# 贡献指南

感谢您对文献阅读器项目的关注！我们欢迎所有形式的贡献。

## 如何贡献

### 报告问题

如果您发现了bug或有功能建议，请：

1. 检查[Issues](https://github.com/yourusername/literature-reader/issues)中是否已有相关问题
2. 如果没有，请创建新Issue，包含：
   - 清晰的问题描述
   - 复现步骤
   - 预期行为
   - 实际行为
   - 环境信息（Python版本、操作系统等）

### 提交代码

1. **Fork项目**
   ```bash
   git clone https://github.com/yourusername/literature-reader.git
   cd literature-reader
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **编写代码**
   - 遵循项目代码规范（PEP8）
   - 添加必要的注释和docstring
   - 确保代码通过lint检查

4. **提交更改**
   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"
   # 或
   git commit -m "fix: 修复bug描述"
   ```

5. **推送并创建Pull Request**
   ```bash
   git push origin feature/your-feature-name
   ```
   然后在GitHub上创建Pull Request

## 代码规范

### Python代码
- 遵循PEP8规范
- 使用4个空格缩进
- 函数和类必须添加docstring
- 使用有意义的变量名

### JavaScript/React代码
- 使用ES6+语法
- 组件使用函数式组件和Hooks
- 使用有意义的变量名
- 保持组件简洁

### 提交信息规范

使用[Conventional Commits](https://www.conventionalcommits.org/)格式：

- `feat:` 新功能
- `fix:` Bug修复
- `docs:` 文档更新
- `style:` 代码格式（不影响功能）
- `refactor:` 代码重构
- `test:` 测试相关
- `chore:` 构建/工具相关

示例：
```
feat: 添加翻译缓存功能
fix: 修复PDF渲染时的内存泄漏
docs: 更新API文档
```

## 开发环境设置

参考[README.md](../README.md)中的"快速开始"部分。

## 测试

在提交PR前，请确保：
- 代码通过lint检查
- 所有测试通过
- 新功能包含测试用例

## 问题反馈

如有任何问题，请通过Issue或邮件联系维护者。

感谢您的贡献！🎉

